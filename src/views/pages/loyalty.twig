{#  LUMINA — Loyalty / rewards (pages/loyalty.twig)  #}
{% extends "layouts.master" %}
{% block content %}
<div class="lumina-container py-8 lg:py-12">
    <header class="text-center max-w-2xl mx-auto mb-10" data-reveal>
        <span class="lumina-eyebrow">{{ trans('loyalty.eyebrow') }}</span>
        <h1 class="lumina-listing-title">{{ page.title | raw }}</h1>
        <p class="lumina-listing-desc mx-auto">{{ trans('loyalty.sub') }}</p>
    </header>
    <div class="max-w-3xl mx-auto" data-reveal>
        <salla-loyalty></salla-loyalty>
    </div>
</div>
{% endblock %}
